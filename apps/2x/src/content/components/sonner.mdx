<Title content="Sonner" />
<Description content="Displays a message to the user in a toast." />
<ComponentLinks
	links={[
		{
			label: "Docs",
			href: "https://sonner.emilkowal.ski/getting-started",
		},
	]}
/>

<ComponentPreview name="sonner-demo" />

## About

[`Sonner`](https://sonner.emilkowal.ski) is created and maintained by [@emilkowalski\_](https://x.com/emilkowalski_).

## Installation

<ComponentInstallation>
<div>
<Steps>

<Step>Run the following command.</Step>

```bash title="Terminal"
npx shadcn@latest add @9ui/sonner
```

<Step>Add Toaster to your app layout.</Step>

```tsx {1,9} title="app/layout.tsx"
import { Toaster } from "@/components/ui/sonner"

export default function RootLayout({ children }) {
	return (
		<html lang="en">
			<head />
			<body>
				<main>{children}</main>
				<Toaster />
			</body>
		</html>
	)
}
```

</Steps>

</div>

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sonner" />

<Step>Add Toaster to your app layout.</Step>

```tsx {1,9} title="app/layout.tsx"
import { Toaster } from "@/components/ui/sonner"

export default function RootLayout({ children }) {
	return (
		<html lang="en">
			<head />
			<body>
				<main>{children}</main>
				<Toaster />
			</body>
		</html>
	)
}
```

</Steps>
</ComponentInstallation>

## Usage

```tsx title="Imports"
import { toast } from "sonner"
```

```tsx title="Toast"
toast("Toast is working!")
```

## Examples

### Promise

<ComponentPreview name="sonner-promise" />

---

### Action

<ComponentPreview name="sonner-action" />

---

### Rich Colors

<ComponentPreview name="sonner-rich-colors" />
