<Title content="Toast" />
<Description content="Displays a message to the user in a toast." />

<ComponentLinks
	links={[
		{
			label: "Docs",
			href: "https://base-ui.com/react/components/toast",
		},
	]}
/>

<ComponentPreview name="toast-demo" />

## Installation

<ComponentInstallation>
<div>
<Steps>

<Step>Run the following command.</Step>

```bash title="Terminal"
npx shadcn@latest add @9ui/toast
```

<Step>Add `ToastProvider` to your app layout.</Step>

```tsx {1,8-10} title="app/layout.tsx"
import { ToastProvider } from "@/components/ui/toast"

export default function RootLayout({ children }) {
	return (
		<html lang="en">
			<head />
			<body>
				<ToastProvider>
					<main>{children}</main>
				</ToastProvider>
			</body>
		</html>
	)
}
```

</Steps>

</div>

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toast" />

<Step>Create `use-toast` hook.</Step>

```tsx title="use-toast.tsx"
"use client"

import { Toast } from "@base-ui-components/react/toast"

export const toastManager = Toast.createToastManager()

export const useToast = Toast.useToastManager
```

<Step>Add `ToastProvider` to your app layout.</Step>

```tsx {1,8-10} title="app/layout.tsx"
import { ToastProvider } from "@/components/ui/toast"

export default function RootLayout({ children }) {
	return (
		<html lang="en">
			<head />
			<body>
				<ToastProvider>
					<main>{children}</main>
				</ToastProvider>
			</body>
		</html>
	)
}
```

</Steps>
</ComponentInstallation>

## Examples

### Promise

<ComponentPreview name="toast-promise" />

---

### Action

<ComponentPreview name="toast-action" />

---

### Rich Colors

<ComponentPreview name="toast-rich-colors" />
